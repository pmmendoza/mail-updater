# Core settings
TZ=Europe/Amsterdam
# Use the production compliance database or point to data/fixtures/compliance_fixture.db for local testing
COMPLIANCE_DB_PATH=../compliance-tracker/compliance.db
PARTICIPANTS_CSV_PATH=../data/participants.csv
WINDOW_DAYS=14
REQUIRED_ACTIVE_DAYS=10
CUTOFF_HOUR_LOCAL=5
SEND_HOUR_LOCAL=9

# SMTP defaults (override with Greenhost values before running real sends)
SMTP_HOST=mail.greenhost.net
SMTP_PORT=587
SMTP_USE_SSL=false
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=Bluesky Feed Project <noreply@example.com>
SMTP_REPLY_TO=
SMTP_DRY_RUN=true
MAIL_SUBJECT=Bluesky Feed Project: daily progress update

# Qualtrics integration
QUALTRICS_BASE_URL=
QUALTRICS_API_TOKEN=
QUALTRICS_SURVEY_FILTER=NEWSFLOWS_pretreat_v1.0

# Output directories
OUTBOX_DIR=outbox
SEND_LOG_PATH=outbox/send_log.jsonl
